require 'spec_helper'

describe VtcPayment do
  it 'has a version number' do
    expect(VtcPayment::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
